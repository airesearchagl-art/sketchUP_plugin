# Smart Framing Tools v1.0.0 — 操作ガイド

この度は **Smart Framing Tools** をご利用いただきありがとうございます。
本ツールは「面の押し出し」「不要な線の消去」「位置合わせの計算」をすべて自動化し、
**たった 2 クリックで部材を整形する**ことを目的に開発されました。

---

## 共通の操作ルール

すべてのツールは **2 ステップ操作**（ステートマシン）で動作します。

| ステップ | 操作 | 画面のヒント |
|---|---|---|
| **1 クリック目** | 基準（境界・カッター）を選択 | ステータスバーに「2 つ目を選択してください」と表示 |
| **2 クリック目** | 対象を選択 → 即座に実行 | 処理結果が画面に反映 |

**ESC キーの動作:**
- 1 クリック目の前に **ESC** → ツールを終了します
- 2 クリック目の待機中に **ESC** → 1 クリック目に戻ります（基準を再選択）

**カラーコード（ホバー時のハイライト）:**
- 🔵 シアン・青 … 境界・基準として選択中
- 🟠 オレンジ・緑 … 操作対象として有効
- 🟣 紫 … 対象が条件を満たしていない（交差なし等）
- ⬜ グレー … ソリッドでないため操作不可

---

## 1. Trim（トリム） — はみ出した部分を切り落とす

**「境界部材にぶつかっているのに、はみ出したまま…」を即解決**

### 操作手順

```
1. カット境界となるソリッド（柱など）の面付近をクリック
   → シアン色でハイライト、カット平面プレビューが表示される

2. 切り落としたい端部（はみ出している側）をクリック
   → ブーリアン演算で境界面より先が除去される
   → 境界ソリッドの選択は維持されるため、次の部材をそのまま続けてクリック可能
```

### ポイント
- **連続トリム対応**: 同じ境界で複数の部材を連続してトリムできます。ESC で境界を再選択。
- 対象部材が境界ソリッドと**交差していない**場合は紫色でハイライトされます。交差するよう部材を移動してから使用してください。
- ソリッドでない部材はグレー表示となり、選択できません。

---

## 2. Split（分割） — 部材を境界で 2 つに切り分ける

**「1 本の梁を、柱の面でスパッと切りたい」**

### 操作手順

```
1. 分割の境界となるソリッド（柱など）の面付近をクリック
   → シアン色でハイライト、カット平面プレビューが表示される

2. 分割したい部材（梁など）をクリック
   → 境界面を挟んで 2 つの独立したソリッドが生成される
```

### ポイント
- 分割後の 2 つの部材はどちらも**ソリッドグループ**として維持されます。
  その後のトリム・延長・別のツールへの入力として引き続き使用できます。
- 内部的には「境界の両側にハーフスペースカッターを生成し、2 回 subtract する」方式で実現しています。
- 分割後も ESC を押さずにそのまま次の部材をクリックすることで、同一境界で続けて分割できます。

---

## 3. Align（位置合わせ） — 面をビシッと揃える

**「バラバラな部材の端部を、ひとつの基準面に揃えたい」**

### 操作手順

```
1. 基準となる面（動かさない側）をクリック
   → 青色でハイライト、カット平面プレビューが表示される

2. 揃えたい面（動かす側）をクリック
   → 部材全体が平行移動し、基準面とツラが合う
```

### ポイント
- Align ツールは**ブーリアン演算を使いません**。部材の形状はそのままで、位置だけを移動します。
  失敗してもソリッドが崩れる心配がなく、最も安全に使えるツールです。
- 移動量は「基準面の平面方程式に対する対象面中心の符号付き距離」として算出されます。
  部材が基準面の奥にあっても手前にあっても、正確に揃います。
- 面の**法線方向（向き）は考慮しません**。法線が逆向きでも同一平面上に移動します。

---

## 4. Extend（延長） — 部材を境界面まで引き伸ばす

**「届いていない柱を、天井や屋根面までスッと伸ばしたい」**

### 操作手順

```
1. 目標となる境界面（天井・梁の面など）をクリック
   → 青色でハイライト、カット平面プレビューが表示される

2. 伸ばしたい部材の「小口（先端の面）」をクリック
   → 境界面を確実に超えるまで PushPull → 境界でブーリアンカット
   → 境界選択は維持されるため、次の部材をそのまま続けてクリック可能
```

### ポイント
- **連続延長対応**: 同じ境界面に対して複数の部材を連続して延長できます。ESC で境界を再選択。
- 部材がすでに境界面を**超えていても**動作します（超えている分だけ切り戻します）。
- **「小口（端面）を狙う」**ことが成功の鍵です（次セクション参照）。

---

## 💡 使いこなしのコツ

### 「小口（こぐち）」を狙えば確実

Trim・Extend ツールには、部材の**先端（端面 = 小口）を優先的に検出するインテリジェンス**が備わっています。

> **小口とは**: 木材の「木口」と同じ意味。部材の断面・切り口のことです。長手方向と垂直な面です。

クリック位置が多少ズレても端面を優先しますが、**部材の先端付近（切り口の面）を意識してクリック**するとより確実に動作します。横面（長手方向の側面）は優先度が低くなるよう設計されています。

### 「ソリッド」が合言葉

本ツールはすべて **SketchUp のソリッドグループ / ソリッドコンポーネント**を対象とします。

エラーが出た場合は、対象部材の**エンティティ情報**（右クリック → エンティティ情報）を確認してください。

| エンティティ情報の表示 | 状態 |
|---|---|
| `ソリッドグループ` / `ソリッドコンポーネント` | ✅ 使用可能 |
| `グループ` / `コンポーネント` | ❌ ソリッドでない（内部に欠けた面・余分な線あり） |

修正方法：
1. グループ内に入り、余分なエッジや開いた面（穴）を確認・修正する
2. 修正後、エンティティ情報で「ソリッド」と表示されれば準備完了

### すべての操作は Undo できます

本ツールの操作はすべて **SketchUp のアンドゥ履歴に記録**されます。
思い通りにならなかった場合は `Ctrl+Z`（Mac: `Cmd+Z`）で元の状態に戻してください。

---

## ⚠ Corner（包絡）ツールについて

> L 字に接合する「包絡（Corner）」機能は、現在 v1.1 で実装予定です。
> それまでは以下の手順で代替できます:
>
> 1. **Extend** で一方の部材を相手まで延長
> 2. **Trim** で飛び出した部分を切り落とす
>
> 2 操作で同等の結果が得られます。

---

## トラブルシューティング

| 症状 | 原因 | 対処法 |
|---|---|---|
| グレー表示でクリックできない | 部材がソリッドでない | エンティティ情報を確認。内部の欠け・余分なエッジを修正 |
| 紫色になる（Trim） | 対象が境界と交差していない | 部材を移動して境界に接触させてから使用 |
| 操作後に形状が崩れる | まれに SketchUp のブーリアン演算が失敗 | Ctrl+Z で取り消し、別の面をクリックして再試行 |
| ツールバーアイコンが見えない | ツールバーが非表示 | **表示** メニュー → **ツールバー** → **Smart Framing Tools** をチェック |
